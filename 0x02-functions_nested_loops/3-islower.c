#include "main.h"

/**
 * _islower - check for lowercase character
 *
 * @ch: character to check
 *
 * Return: 1 if ch is lowercase, 0 otherwise
 */
int _islower(int)
{
	return (ch >= 'a' && ch <= 'z');
}
